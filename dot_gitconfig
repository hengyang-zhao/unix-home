# vim: set ft=gitconfig:
#

# PLEASE PUT USER SPECS INTO ~/.site_env/gitconfig
#
#[user]
#	name = Dr. Who
#	email = dr.who@example.com
#


[color]
	ui = auto

[core]
	editor = vim

[alias]
	b = branch
	br = branch -a -vv
	co = checkout
	f = log --pretty=fuller
	h = log --pretty=format:'%C(yellow)%h%C(reset)%C(blue)%d%C(reset) %C(white)%s%C(reset) %C(dim)-- %an (%ar)%C(reset)' --graph --abbrev-commit
	hi = log --graph
	s = status -s
	cb = checkout -b
	re = remote -v
	cl = clone --recursive
	wdiff = diff --color-words

[push]
	default = current

[include]
	path = ~/.site_env/gitconfig
